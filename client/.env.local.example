# run supabase/schema.sql and enable anonymous sign-ins under Authentication → Providers
# without these the app falls back to localStorage
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# API server, defaults to http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3001
